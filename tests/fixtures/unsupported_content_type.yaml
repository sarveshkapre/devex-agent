openapi: 3.0.0
info:
  title: Unsupported Content Types API
  version: "1.0"
paths:
  /echo:
    post:
      summary: Echo text
      requestBody:
        required: true
        content:
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: ok
          content:
            text/plain:
              schema:
                type: string
