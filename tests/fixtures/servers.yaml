openapi: 3.0.0
info:
  title: Servers Example
  version: "1.0"
servers:
  - url: https://{region}.api.example.com/{basePath}
    variables:
      region:
        default: us
      basePath:
        default: v1
  - url: https://eu.api.example.com/v1
paths:
  /pets:
    get:
      summary: List pets
      responses:
        "200":
          description: ok
