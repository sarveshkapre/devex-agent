openapi: 3.0.3
info:
  title: Malformed Paths API
  version: 1.0.0
paths:
  /valid:
    get:
      summary: Valid endpoint
      responses:
        "200":
          description: OK
  /broken-path-item: invalid
  /broken-operation:
    get: invalid
